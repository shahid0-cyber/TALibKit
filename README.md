# TALibKit
Repo setup that auto-detects new TA-Lib releases, builds an XCFramework, publishes it, and rewrites your README/Package.swift with the new link and checksum, all based on the official TA-Lib/ta-lib repo's active release cadence
